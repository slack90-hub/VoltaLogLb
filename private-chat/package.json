{
  "name": "mds-private-room",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "test": "node scripts/build.mjs && node --test test/*.test.mjs",
    "build": "node scripts/build.mjs",
    "check": "npm run build && wrangler deploy --dry-run",
    "provision": "node scripts/provision.mjs",
    "predev": "node scripts/build.mjs",
    "test:browser": "node scripts/build.mjs && node test/browser.mjs && node test/passphrase-browser.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "miniflare": "^5.20260918.0-alpha",
    "wrangler": "^4.135.0"
  },
  "engines": {
    "node": ">=22"
  }
}
